'use client';

/**
 * 交易系统管理页面
 * 
 * 路径: /admin/trading
 * 
 * 功能模块:
 * - 策略管理: 交易策略配置、参数调整、回测结果
 * - 交易监控: 实时持仓、订单队列、执行日志
 * - 风险控制: 仓位管理、止损设置、风险指标
 * - 绩效分析: 收益曲线、胜率统计、夏普比率
 */

import React, { useState } from 'react';
import TradingStrategyPanel from '@/app/components/trading/TradingStrategyPanel';
import TradingMonitorPanel from '@/app/components/trading/TradingMonitorPanel';
import RiskControlPanel from '@/app/components/trading/RiskControlPanel';
import PerformanceAnalysisPanel from '@/app/components/trading/PerformanceAnalysisPanel';

type TabType = 'strategy' | 'monitor' | 'risk' | 'performance';

export default function TradingSystemPage() {
  const [activeTab, setActiveTab] = useState<TabType>('strategy');

  return (
    <div className="space-y-6">
      {/* 页头 */}
      <div>
        <h1 className="text-2xl font-semibold text-gray-900 mb-2">
          📊 交易系统管理
        </h1>
        <p className="text-gray-600">
          策略配置、交易监控、风险控制、绩效分析
        </p>
      </div>

      {/* Tab切换 */}
      <div className="flex gap-2 border-b border-gray-200">
        <button
          onClick={() => setActiveTab('strategy')}
          className={`px-4 py-3 font-medium text-sm transition-colors border-b-2 ${
            activeTab === 'strategy'
              ? 'text-blue-600 border-blue-600'
              : 'text-gray-600 border-transparent hover:text-blue-600'
          }`}
        >
          🎯 策略管理
        </button>
        <button
          onClick={() => setActiveTab('monitor')}
          className={`px-4 py-3 font-medium text-sm transition-colors border-b-2 ${
            activeTab === 'monitor'
              ? 'text-blue-600 border-blue-600'
              : 'text-gray-600 border-transparent hover:text-blue-600'
          }`}
        >
          📈 交易监控
        </button>
        <button
          onClick={() => setActiveTab('risk')}
          className={`px-4 py-3 font-medium text-sm transition-colors border-b-2 ${
            activeTab === 'risk'
              ? 'text-blue-600 border-blue-600'
              : 'text-gray-600 border-transparent hover:text-blue-600'
          }`}
        >
          🛡️ 风险控制
        </button>
        <button
          onClick={() => setActiveTab('performance')}
          className={`px-4 py-3 font-medium text-sm transition-colors border-b-2 ${
            activeTab === 'performance'
              ? 'text-blue-600 border-blue-600'
              : 'text-gray-600 border-transparent hover:text-blue-600'
          }`}
        >
          📉 绩效分析
        </button>
      </div>

      {/* Tab内容 */}
      <div className="min-h-[400px]">
        {activeTab === 'strategy' && <TradingStrategyPanel />}
        {activeTab === 'monitor' && <TradingMonitorPanel />}
        {activeTab === 'risk' && <RiskControlPanel />}
        {activeTab === 'performance' && <PerformanceAnalysisPanel />}
      </div>
    </div>
  );
}

