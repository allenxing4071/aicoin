# 版本管理指南

## 📋 概述

本文档说明 AIcoin 项目的版本管理规范和自动化流程。

---

## 🏷️ 版本号规范

### 语义化版本 (Semantic Versioning)

格式: `MAJOR.MINOR.PATCH` (例如: `3.2.0`)

- **MAJOR (主版本号)**: 不兼容的 API 变更
- **MINOR (次版本号)**: 向后兼容的功能新增
- **PATCH (修订号)**: 向后兼容的问题修正

### 示例

- `3.0.0` → `3.1.0`: 新增功能（交易所接入）
- `3.1.0` → `3.1.1`: Bug 修复
- `3.1.0` → `3.2.0`: 新增功能（样式统一、性能优化）
- `3.2.0` → `4.0.0`: 重大架构变更

---

## 📁 版本号存储位置

### 1. VERSION 文件
**位置**: `/VERSION`  
**内容**: 纯文本版本号
```
3.2.0
```

### 2. 后端配置
**位置**: `/backend/app/core/config.py`
```python
APP_VERSION: str = "3.2.0"
```

### 3. 前端配置
**位置**: `/frontend/package.json`
```json
{
  "version": "3.2.0"
}
```

### 4. README 文档
**位置**: `/README.md`
```markdown
![Version](https://img.shields.io/badge/version-3.2.0-blue)
```

---

## 🔄 自动更新版本号

### 使用脚本更新

```bash
# 更新到新版本
./scripts/utils/update_version.sh 3.3.0
```

### 脚本功能

1. ✅ 验证版本号格式
2. ✅ 更新 VERSION 文件
3. ✅ 更新后端配置
4. ✅ 更新前端配置
5. ✅ 更新 README
6. ✅ 创建 Git 标签（可选）
7. ✅ 推送到远程（可选）

### 脚本输出示例

```bash
🔄 开始更新版本号到 v3.3.0...

📝 更新 VERSION 文件...
  ✅ VERSION

📝 更新后端配置...
  ✅ backend/app/core/config.py

📝 更新前端 package.json...
  ✅ frontend/package.json

📝 更新 README.md...
  ✅ README.md

🏷️  创建 Git 标签...
  当前分支: main
是否创建 Git 标签 v3.3.0? (y/n) y
  ✅ 已创建标签 v3.3.0

🎉 版本号更新完成！
```

---

## 📝 版本发布流程

### 1. 确定版本号

根据变更类型确定新版本号：

```bash
# 查看当前版本
cat VERSION

# 查看变更内容
git log --oneline v3.2.0..HEAD
```

### 2. 更新版本号

```bash
# 使用脚本自动更新
./scripts/utils/update_version.sh 3.3.0
```

### 3. 更新 CHANGELOG

编辑 `CHANGELOG.md`，添加新版本的变更记录：

```markdown
## [3.3.0] - 2025-11-10

### Added
- 新增功能 A
- 新增功能 B

### Changed
- 优化功能 C
- 改进功能 D

### Fixed
- 修复 Bug E
- 修复 Bug F
```

### 4. 提交变更

```bash
# 查看变更
git diff

# 提交版本更新
git add .
git commit -m "chore: bump version to 3.3.0"

# 推送到远程
git push origin main
```

### 5. 创建发布标签

```bash
# 创建标签
git tag -a v3.3.0 -m "Release version 3.3.0"

# 推送标签
git push origin v3.3.0
```

### 6. 重新构建部署

```bash
# 重新构建
docker compose build

# 重启服务
docker compose up -d

# 验证版本
curl http://localhost:8000/docs
```

---

## 🔍 版本查询

### 查看当前版本

```bash
# 方法1: 查看 VERSION 文件
cat VERSION

# 方法2: 查看后端配置
grep APP_VERSION backend/app/core/config.py

# 方法3: 查看前端配置
grep version frontend/package.json | head -1

# 方法4: 查看 Git 标签
git describe --tags --abbrev=0
```

### API 查询版本

访问 API 文档页面，版本号显示在页面顶部：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

## 📊 版本历史

### v3.2.0 (2025-11-09)

**主要变更**:
- ✅ 创建统一组件库（6个可复用组件）
- ✅ 完成页面样式统一（10+页面）
- ✅ 性能优化（前端+后端+Docker）
- ✅ 代码质量提升（减少93%重复代码）

**详细文档**: `docs/99-归档/v3.2-样式统一/`

### v3.1.0 (2025-11-08)

**主要变更**:
- ✅ 新增交易所接入功能
- ✅ 完善 AI 决策系统
- ✅ 优化数据库结构

### v3.0.0 (2025-11-01)

**主要变更**:
- ✅ 项目初始化
- ✅ 核心功能实现

---

## 🛠️ 手动更新版本号

如果不使用自动脚本，可以手动更新：

### 1. 更新 VERSION 文件
```bash
echo "3.3.0" > VERSION
```

### 2. 更新后端配置
编辑 `backend/app/core/config.py`:
```python
APP_VERSION: str = "3.3.0"
```

### 3. 更新前端配置
编辑 `frontend/package.json`:
```json
{
  "version": "3.3.0"
}
```

### 4. 更新 README
编辑 `README.md`:
```markdown
![Version](https://img.shields.io/badge/version-3.3.0-blue)
```

---

## 📌 注意事项

### 1. 版本号一致性
确保所有位置的版本号保持一致：
- VERSION 文件
- 后端配置
- 前端配置
- README 文档

### 2. Git 标签
- 标签格式: `v3.2.0`（带 v 前缀）
- 标签消息: `Release version 3.2.0`
- 推送标签: `git push origin v3.2.0`

### 3. 重新构建
版本号更新后，必须重新构建 Docker 镜像：
```bash
docker compose build backend frontend
docker compose up -d
```

### 4. 文档更新
每次版本发布都应该更新：
- CHANGELOG.md
- 相关功能文档
- API 文档（如有变更）

---

## 🚀 快速参考

### 发布新版本（完整流程）

```bash
# 1. 更新版本号
./scripts/utils/update_version.sh 3.3.0

# 2. 更新 CHANGELOG
vim CHANGELOG.md

# 3. 提交变更
git add .
git commit -m "chore: bump version to 3.3.0"
git push origin main

# 4. 创建标签
git tag -a v3.3.0 -m "Release version 3.3.0"
git push origin v3.3.0

# 5. 重新构建
docker compose build
docker compose up -d
```

### 回滚到旧版本

```bash
# 1. 检出旧版本标签
git checkout v3.2.0

# 2. 重新构建
docker compose build
docker compose up -d
```

---

## 📚 相关文档

- [部署指南](./01-部署指南.md)
- [Docker 使用指南](./02-Docker使用指南.md)
- [监控和日志](./03-监控和日志.md)

---

*最后更新: 2025-11-09*  
*当前版本: v3.2.0*

