# 🎯 主网操作指南（$200资金）

## ⚡ 快速开始

### 1. 系统已启动 ✅
```bash
# 检查状态
docker ps | grep aicoin

# 应该看到4个容器运行中:
# - aicoin-backend-prod-v2
# - aicoin-postgres-prod
# - aicoin-redis-prod
# - aicoin-qdrant-prod
```

### 2. 立即充值 ⚠️
```
钱包地址: 0x91BF94f0A746657603B6E827c03B2B919e13C41e
充值金额: $200 USDC
充值网址: https://app.hyperliquid.xyz/
```

### 3. 验证充值
```bash
# 查看余额
curl http://localhost:8000/api/v1/account/info | python3 -m json.tool

# 应该显示余额约$200
```

---

## 📊 实时监控

### 方法1: 实时日志（推荐）
```bash
# 查看所有日志
docker logs -f aicoin-backend-prod-v2

# 只看决策
docker logs -f aicoin-backend-prod-v2 2>&1 | grep "决策"

# 只看交易
docker logs -f aicoin-backend-prod-v2 2>&1 | grep "交易"
```

### 方法2: API查询
```bash
# 系统状态
curl http://localhost:8000/health | python3 -m json.tool

# 账户信息
curl http://localhost:8000/api/v1/account/info | python3 -m json.tool

# 持仓信息
curl http://localhost:8000/api/v1/account/positions | python3 -m json.tool
```

### 方法3: 监控脚本
```bash
# 启动监控
./scripts/monitor_system.sh
```

---

## 🛡️ 风控设置（已配置）

### L1级别（最保守）
```
✅ 单仓位: 10% ($20)
✅ 最大杠杆: 2x
✅ 最低置信度: 80%
✅ 每日交易: 最多3笔
✅ 决策间隔: 5分钟
✅ 止损: 5%
✅ 止盈: 10%
```

### 自动保护
```
✅ 单日亏损>5% → 停止交易
✅ 总回撤>15% → 停止交易
✅ 账户<$150 → 停止交易
✅ 低于$180 → 发送告警
```

---

## 🤖 AI决策流程

### 每5分钟自动执行
```
1. 获取市场数据
2. AI生成决策
3. 检查置信度（需要≥80%）
4. 检查风控约束
5. 执行或拒绝交易
6. 记录结果
```

### 决策结果类型
```
✅ 通过 → 执行交易
❌ 拒绝 → 置信度不足
🚫 拒绝 → 触发风控
🔒 拒绝 → 权限限制
```

---

## 📈 预期表现

### 正常情况
```
每天: 0-3笔交易
每周: 5-15笔交易
月收益: 2-10%
最大回撤: <10%
```

### 如果出现
```
⚠️ 连续3次被拒 → 正常（市场机会少）
⚠️ 单日无交易 → 正常（等待高质量机会）
❌ 单日亏损>5% → 系统自动停止
❌ 账户<$150 → 系统自动停止
```

---

## 🚨 紧急情况处理

### 立即停止系统
```bash
# 方法1: 停止Docker
./scripts/stop_system_emergency.sh

# 方法2: 手动停止
docker stop aicoin-backend-prod-v2
```

### 手动平仓
```
1. 访问 https://app.hyperliquid.xyz/
2. 连接钱包
3. 手动平掉所有持仓
```

---

## 📝 日常检查（每天）

### 早上检查
```bash
# 1. 查看系统状态
curl http://localhost:8000/health

# 2. 查看账户余额
curl http://localhost:8000/api/v1/account/info

# 3. 查看昨日交易
docker logs aicoin-backend-prod-v2 2>&1 | grep "交易" | tail -20
```

### 晚上检查
```bash
# 1. 查看今日收益
curl http://localhost:8000/api/v1/account/info

# 2. 查看决策统计
curl http://localhost:8000/health | grep "total_decisions"

# 3. 备份日志
docker logs aicoin-backend-prod-v2 > logs/mainnet_$(date +%Y%m%d).log
```

---

## 🔧 常用命令

### 系统管理
```bash
# 查看状态
docker ps | grep aicoin

# 重启后端
docker restart aicoin-backend-prod-v2

# 查看日志
docker logs -f aicoin-backend-prod-v2

# 停止系统
docker-compose -f deploy/docker-compose.prod.yml --env-file .env.prod down
```

### 数据查询
```bash
# 健康检查
curl http://localhost:8000/health

# 账户信息
curl http://localhost:8000/api/v1/account/info

# 持仓信息
curl http://localhost:8000/api/v1/account/positions

# API文档
open http://localhost:8000/docs
```

---

## ⏰ 时间线

### 第1天
- [x] 系统启动
- [ ] 充值$200
- [ ] 观察首次决策
- [ ] 观察首次交易（如果有）

### 第1周
- [ ] 每天检查账户
- [ ] 记录所有交易
- [ ] 评估AI决策质量
- [ ] 评估风控效果

### 第1月
- [ ] 计算月收益率
- [ ] 计算夏普比率
- [ ] 计算最大回撤
- [ ] 决定是否继续

---

## 📞 获取帮助

### 查看文档
```bash
# 主网启动报告
cat docs/主网启动报告.md

# 快速开始
cat docs/00-快速开始/QUICKSTART.md

# 部署指南
cat docs/07-部署运维/部署指南.md
```

### 查看日志
```bash
# 最近100行
docker logs aicoin-backend-prod-v2 2>&1 | tail -100

# 错误日志
docker logs aicoin-backend-prod-v2 2>&1 | grep ERROR

# 决策日志
docker logs aicoin-backend-prod-v2 2>&1 | grep "决策"
```

---

## ✅ 检查清单

### 启动前
- [x] Docker服务运行
- [x] 主网配置完成
- [x] 风控参数设置
- [x] AI系统启动

### 运行中
- [ ] 账户已充值$200
- [ ] 每天检查余额
- [ ] 每天检查交易
- [ ] 每周评估表现

### 异常处理
- [ ] 知道如何紧急停止
- [ ] 知道如何手动平仓
- [ ] 知道如何查看日志
- [ ] 知道如何重启系统

---

**当前状态**: 🟢 系统运行中，等待充值

**下一步**: 充值$200到钱包地址 `0x91BF94f0A746657603B6E827c03B2B919e13C41e`

**祝交易顺利！** 🚀💰

