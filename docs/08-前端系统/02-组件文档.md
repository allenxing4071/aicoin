# 🧩 React组件文档

> AIcoin前端组件详细说明  
> **最后更新**: 2025-11-02

---

## 📚 组件总览

AIcoin前端共有**35+个React组件**，分为7大类：

| 类别 | 组件数 | 说明 |
|------|--------|------|
| AI相关 | 4个 | AI状态、决策、权限 |
| 图表 | 4个 | K线、净值曲线 |
| 交易 | 2个 | 交易列表、持仓 |
| 性能 | 3个 | 性能仪表盘、指标卡 |
| 通用 | 3个 | 图标、Logo、骨架屏 |
| 管理后台 | 2个 | 数据表格、筛选栏 |
| 页面 | 3个 | 主页、模型详情、管理后台 |

---

## 1. AI相关组件

### 1.1 AIStatusPanel - AI状态面板

**文件**: `app/components/ai/AIStatusPanel.tsx`

**功能**: 显示AI系统的实时状态

**Props**: 无

**状态**:
```typescript
const [status, setStatus] = useState({
  orchestrator: '运行中',
  mode: '观察模式',
  lastDecision: '5分钟前'
});
```

**API调用**:
```typescript
// GET /api/v1/status
{
  orchestrator_status: "active",
  trade_count: 0,
  last_decision: "2025-11-02T10:00:00Z"
}
```

**使用示例**:
```tsx
import { AIStatusPanel } from '@/app/components/ai/AIStatusPanel';

<AIStatusPanel />
```

**显示内容**:
- Orchestrator状态（运行中/停止）
- 运行模式（观察/交易）
- 最后决策时间
- 硬约束状态
- 软约束状态
- 记忆系统状态

---

### 1.2 PermissionIndicator - 权限指示器

**文件**: `app/components/ai/PermissionIndicator.tsx`

**功能**: 显示当前AI权限等级（L0-L5）

**Props**: 无

**状态**:
```typescript
const [permission, setPermission] = useState({
  level: 'L2',
  description: 'Cautious Trader',
  max_position_size: 0.15
});
```

**API调用**:
```typescript
// GET /api/v1/ai/permission
{
  level: "L2",
  description: "Cautious Trader",
  max_position_size: 0.15,
  confidence_threshold: 0.70
}
```

**使用示例**:
```tsx
import { PermissionIndicator } from '@/app/components/ai/PermissionIndicator';

<PermissionIndicator />
```

**可视化**:
- 进度条显示权限等级
- 颜色编码（L0灰色 → L5绿色）
- 权限限制说明
- 升级/降级条件

---

### 1.3 DecisionTimeline - 决策时间轴

**文件**: `app/components/ai/DecisionTimeline.tsx`

**功能**: 显示AI决策历史时间轴

**Props**:
```typescript
interface Props {
  filter?: string;  // 模型筛选
}
```

**状态**:
```typescript
const [decisions, setDecisions] = useState([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState(null);
const [retryCount, setRetryCount] = useState(0);
```

**API调用**:
```typescript
// GET /api/v1/ai/decisions?limit=50
{
  decisions: [
    {
      id: 1,
      timestamp: "2025-11-02T10:00:00Z",
      action: "HOLD",
      confidence: 0.3,
      reasoning: "..."
    }
  ]
}
```

**使用示例**:
```tsx
import { DecisionTimeline } from '@/app/components/ai/DecisionTimeline';

<DecisionTimeline filter="deepseek-chat-v3.1" />
```

**特性**:
- ✅ 自动重试机制（3次）
- ✅ 加载骨架屏
- ✅ 错误处理
- ✅ 手动重试按钮

---

### 1.4 DecisionFlowViewer - 决策流程查看器

**文件**: `app/components/ai/DecisionFlowViewer.tsx`

**功能**: 可视化决策流程

**Props**: 无

**使用场景**: 调试和理解AI决策过程

---

## 2. 图表组件

### 2.1 TradingChart - 交易图表

**文件**: `app/components/charts/TradingChart.tsx`

**功能**: 显示K线图（使用Recharts）

**Props**:
```typescript
interface Props {
  symbol: string;
  interval?: string;
}
```

**使用示例**:
```tsx
import { TradingChart } from '@/app/components/charts/TradingChart';

<TradingChart symbol="BTC" interval="1h" />
```

---

### 2.2 LightweightChart - 轻量级图表

**文件**: `app/components/charts/LightweightChart.tsx`

**功能**: 使用TradingView Lightweight Charts显示K线

**Props**:
```typescript
interface Props {
  symbol: string;
}
```

**配置**:
```typescript
const chartOptions = {
  layout: {
    textColor: '#1a1a1a',
    background: { color: '#ffffff' }
  },
  leftPriceScale: {
    visible: true,
    borderColor: '#2B2B43',
    invertScale: true  // Y轴反转
  },
  timeScale: {
    timeVisible: true,
    borderColor: '#2B2B43'
  },
  grid: {
    vertLines: { color: '#e0e0e0' },
    horzLines: { color: '#e0e0e0' }
  }
};
```

**使用示例**:
```tsx
import { LightweightChart } from '@/app/components/charts/LightweightChart';

<LightweightChart symbol="BTC" />
```

---

### 2.3 MultiModelChart - 多模型图表

**文件**: `app/components/charts/MultiModelChart.tsx`

**功能**: 显示多个AI模型的净值曲线

**Props**: 无

**使用示例**:
```tsx
import { MultiModelChart } from '@/app/components/charts/MultiModelChart';

<MultiModelChart />
```

---

### 2.4 EquityCurveMulti - 多模型净值曲线

**文件**: `app/components/charts/EquityCurveMulti.tsx`

**功能**: 显示多个模型的净值对比

**Props**: 无

---

## 3. 交易组件

### 3.1 TradeListComplete - 交易列表

**文件**: `app/components/trades/TradeListComplete.tsx`

**功能**: 显示已完成的交易记录

**Props**:
```typescript
interface Props {
  selectedModel: string;
  models: Model[];
}
```

**状态**:
```typescript
const [trades, setTrades] = useState([]);
const [loading, setLoading] = useState(true);
```

**API调用**:
```typescript
// GET /api/v1/trading/trades?limit=100
{
  success: true,
  trades: [
    {
      id: "order_123",
      model: "deepseek-chat-v3.1",
      side: "buy",
      symbol: "BTC",
      price: "110000.0",
      size: "0.01",
      closed_pnl: "10.5"
    }
  ]
}
```

**使用示例**:
```tsx
import { TradeListComplete } from '@/app/components/trades/TradeListComplete';

<TradeListComplete 
  selectedModel="deepseek-chat-v3.1"
  models={models}
/>
```

**特性**:
- ✅ 30秒自动刷新
- ✅ 做多/做空标识
- ✅ 盈亏计算
- ✅ 持仓时间格式化
- ✅ 模型筛选

**数据格式化**:
```typescript
// 价格格式化
price: `$${parseFloat(trade.price || 0).toFixed(4)}`

// 数量格式化
quantity: parseFloat(trade.size || 0).toFixed(4)

// 盈亏格式化
pnl: parseFloat(trade.closed_pnl || 0)
```

---

### 3.2 PositionsList - 持仓列表

**文件**: `app/components/positions/PositionsList.tsx`

**功能**: 显示当前持仓

**Props**: 无

**状态**:
```typescript
const [positions, setPositions] = useState([]);
const [loading, setLoading] = useState(true);
```

**API调用**:
```typescript
// GET /api/v1/trading/positions
{
  success: true,
  positions: [
    {
      coin: "BTC",
      side: "long",
      size: 0.01,
      entry_price: 110000.0,
      unrealized_pnl: 0.105
    }
  ]
}
```

**使用示例**:
```tsx
import { PositionsList } from '@/app/components/positions/PositionsList';

<PositionsList />
```

**特性**:
- ✅ 10秒自动刷新
- ✅ 未实现盈亏显示
- ✅ 杠杆显示
- ✅ 按模型分组

---

## 4. 性能组件

### 4.1 PerformanceDashboard - 性能仪表盘

**文件**: `app/components/performance/PerformanceDashboard.tsx`

**功能**: 显示系统性能指标

**Props**: 无

**状态**:
```typescript
const [metrics, setMetrics] = useState(null);
const [loading, setLoading] = useState(true);
const [retryCount, setRetryCount] = useState(0);
```

**API调用**:
```typescript
// GET /api/v1/performance/metrics
{
  total_return: 0.155,
  sharpe_ratio: 1.85,
  max_drawdown: 0.085,
  win_rate: 0.62
}
```

**使用示例**:
```tsx
import { PerformanceDashboard } from '@/app/components/performance/PerformanceDashboard';

<PerformanceDashboard />
```

**显示指标**:
- 收益指标（总收益、年化收益）
- 风险指标（最大回撤、波动率）
- 交易统计（总交易、胜率）
- 风险调整收益（夏普、索提诺）

---

### 4.2 MetricCard - 指标卡片

**文件**: `app/components/performance/MetricCard.tsx`

**功能**: 显示单个性能指标

**Props**:
```typescript
interface Props {
  title: string;
  value: number;
  format?: 'percent' | 'number' | 'currency';
  trend?: 'up' | 'down' | 'neutral';
}
```

**使用示例**:
```tsx
import { MetricCard } from '@/app/components/performance/MetricCard';

<MetricCard 
  title="总收益"
  value={0.155}
  format="percent"
  trend="up"
/>
```

---

### 4.3 RiskGauge - 风险仪表

**文件**: `app/components/performance/RiskGauge.tsx`

**功能**: 可视化风险指标

**Props**:
```typescript
interface Props {
  value: number;
  max: number;
  label: string;
  inverse?: boolean;  // 是否反转（越低越好）
}
```

**使用示例**:
```tsx
import { RiskGauge } from '@/app/components/performance/RiskGauge';

<RiskGauge 
  value={0.085}
  max={0.20}
  label="最大回撤"
  inverse={true}
/>
```

**颜色编码**:
```typescript
// 正常指标（越高越好）
value < 30%  → 红色
value < 70%  → 黄色
value >= 70% → 绿色

// 反转指标（越低越好）
value < 30%  → 绿色
value < 70%  → 黄色
value >= 70% → 红色
```

---

## 5. 通用组件

### 5.1 CoinIcon - 币种图标

**文件**: `app/components/common/CoinIcon.tsx`

**功能**: 显示加密货币SVG图标

**Props**:
```typescript
interface Props {
  symbol: string;  // BTC, ETH, SOL, BNB, DOGE, XRP
  size?: number;   // 默认24
}
```

**使用示例**:
```tsx
import { CoinIcon } from '@/app/components/common/CoinIcon';

<CoinIcon symbol="BTC" size={32} />
```

**支持的币种**:
- BTC - Bitcoin
- ETH - Ethereum
- SOL - Solana
- BNB - Binance Coin
- DOGE - Dogecoin
- XRP - Ripple

**实现方式**: 直接嵌入SVG代码，无需外部文件

---

### 5.2 DeepSeekLogo - DeepSeek Logo

**文件**: `app/components/common/DeepSeekLogo.tsx`

**功能**: 显示DeepSeek鲸鱼Logo

**Props**:
```typescript
interface Props {
  size?: number;  // 默认24
}
```

**使用示例**:
```tsx
import { DeepSeekLogo } from '@/app/components/common/DeepSeekLogo';

<DeepSeekLogo size={32} />
```

**实现方式**: 使用`next/image`加载`/deepseek_logo.png`

---

### 5.3 LoadingSkeleton - 加载骨架屏

**文件**: `app/components/common/LoadingSkeleton.tsx`

**功能**: 显示加载动画占位符

**Props**:
```typescript
interface Props {
  type: 'text' | 'card' | 'table' | 'chart';
  rows?: number;  // 表格行数
}
```

**使用示例**:
```tsx
import { LoadingSkeleton } from '@/app/components/common/LoadingSkeleton';

// 文本骨架
<LoadingSkeleton type="text" />

// 卡片骨架
<LoadingSkeleton type="card" />

// 表格骨架
<LoadingSkeleton type="table" rows={5} />
```

---

## 6. 管理后台组件

### 6.1 DataTable - 数据表格

**文件**: `app/components/admin/DataTable.tsx`

**功能**: 通用数据表格组件

**Props**:
```typescript
interface Props {
  data: any[];
  columns: Column[];
  onSort?: (column: string) => void;
  sortColumn?: string;
  sortDirection?: 'asc' | 'desc';
}
```

**使用示例**:
```tsx
import { DataTable } from '@/app/components/admin/DataTable';

<DataTable 
  data={trades}
  columns={[
    { key: 'id', label: 'ID' },
    { key: 'symbol', label: '币种' },
    { key: 'price', label: '价格', format: 'currency' }
  ]}
  onSort={handleSort}
/>
```

---

### 6.2 FilterBar - 筛选栏

**文件**: `app/components/admin/FilterBar.tsx`

**功能**: 数据筛选组件

**Props**:
```typescript
interface Props {
  filters: Filter[];
  onFilterChange: (filters: any) => void;
}
```

**使用示例**:
```tsx
import { FilterBar } from '@/app/components/admin/FilterBar';

<FilterBar 
  filters={[
    { key: 'symbol', label: '币种', type: 'select', options: ['BTC', 'ETH'] },
    { key: 'side', label: '方向', type: 'select', options: ['buy', 'sell'] }
  ]}
  onFilterChange={handleFilterChange}
/>
```

---

## 7. 页面组件

### 7.1 主页 - Home

**文件**: `app/page.tsx`

**功能**: 主交易监控页面

**状态管理**:
```typescript
const [activeTab, setActiveTab] = useState('trades');
const [selectedModel, setSelectedModel] = useState('all');
const [modelsData, setModelsData] = useState([]);
const [aiHealth, setAiHealth] = useState({});
```

**Tab切换**:
- 已完成交易
- 模型对话
- 持仓
- AI状态
- 决策历史
- 性能仪表盘

---

### 7.2 模型详情页

**文件**: `app/models/[slug]/page.tsx`

**功能**: 显示单个AI模型的详细信息

**动态路由**: `/models/deepseek-chat-v3.1`

**状态管理**:
```typescript
const [modelData, setModelData] = useState(null);
const [activeTab, setActiveTab] = useState('overview');
```

**Tab切换**:
- 概览
- 交易历史
- 决策记录

---

### 7.3 管理后台

**文件**: `app/admin/page.tsx`

**功能**: 数据库查看器

**页面**:
- `/admin` - 概览
- `/admin/trades` - 交易记录
- `/admin/positions` - 持仓记录
- `/admin/ai-decisions` - AI决策日志
- `/admin/memory` - 记忆系统

---

## 8. 组件开发规范

### 8.1 命名规范

```typescript
// 组件：PascalCase
export const PriceTicker = () => {};

// Props接口：PascalCase + Props后缀
interface PriceTickerProps {
  symbol: string;
}

// 状态：camelCase
const [isLoading, setIsLoading] = useState(false);

// 函数：camelCase
const fetchRealPrices = async () => {};
```

### 8.2 文件结构

```typescript
// 1. 导入
import React, { useState, useEffect } from 'react';
import axios from 'axios';

// 2. 类型定义
interface Props {
  // ...
}

// 3. 常量
const API_BASE = 'http://localhost:8000/api/v1';

// 4. 组件
export const ComponentName = ({ prop1, prop2 }: Props) => {
  // 4.1 状态
  const [state, setState] = useState();
  
  // 4.2 副作用
  useEffect(() => {
    // ...
  }, []);
  
  // 4.3 事件处理
  const handleClick = () => {
    // ...
  };
  
  // 4.4 渲染
  return (
    <div>
      {/* ... */}
    </div>
  );
};
```

### 8.3 错误处理模式

```typescript
try {
  const response = await axios.get(url);
  setData(response.data);
  setLoading(false);
} catch (error) {
  console.error('Failed to fetch:', error);
  setLoading(true);  // 保持加载状态
}
```

### 8.4 加载状态

```typescript
// 初始状态
const [loading, setLoading] = useState(true);

// 加载中
if (loading) {
  return <LoadingSkeleton type="card" />;
}

// 无数据
if (data.length === 0) {
  return <div>暂无数据</div>;
}

// 有数据
return <div>{/* 渲染数据 */}</div>;
```

---

## 9. 性能优化

### 9.1 React.memo

```typescript
export const ExpensiveComponent = React.memo(({ data }) => {
  return <div>{/* ... */}</div>;
});
```

### 9.2 useCallback

```typescript
const handleClick = useCallback(() => {
  // ...
}, [dependency]);
```

### 9.3 useMemo

```typescript
const expensiveValue = useMemo(() => {
  return computeExpensiveValue(data);
}, [data]);
```

---

## 10. 测试建议

### 10.1 单元测试

```typescript
import { render, screen } from '@testing-library/react';
import { PriceTicker } from './PriceTicker';

test('renders price ticker', () => {
  render(<PriceTicker />);
  expect(screen.getByText(/BTC/i)).toBeInTheDocument();
});
```

### 10.2 集成测试

```typescript
test('fetches and displays data', async () => {
  render(<TradeListComplete />);
  await waitFor(() => {
    expect(screen.getByText(/交易记录/i)).toBeInTheDocument();
  });
});
```

---

**文档版本**: v1.0  
**最后更新**: 2025-11-02  
**维护状态**: ✅ Active

